# CipherFS v1.4.0 - Update Mechanism Fix

## Bug Fixes
*   **Update Target Fix**: Fixed an issue where the `update` command failed with "No asset found for target" by explicitly targeting the raw `cipherfs` binary in GitHub releases. This ensures reliable updates across Linux distributions without requiring specific target triple matches for the pre-compiled binary.

## Improvements
*   Internal configuration adjustments to streamline the self-update process.

## Maintenance
*   Full compatibility with existing v1.3.0 features.
