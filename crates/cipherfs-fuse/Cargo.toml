[package]
name = "cipherfs-fuse"
version.workspace = true
edition.workspace = true
license.workspace = true

[target.'cfg(unix)'.dependencies]
anyhow.workspace = true
cipherfs-core = { path = "../cipherfs-core" }
fuser = { version = "0.18.0", default-features = false }
libc.workspace = true
